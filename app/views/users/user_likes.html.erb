<div class="background">
  <div class="main-frame">
    <div class="profile-post">
      <div class="profile-post-title">
        <h4>ー <%= @user.user_name %>さんのいいねした投稿一覧(<%= @like_comments.count %>) ー</h4>
      </div>
      <%= render partial: 'shared/user/user_comments', collection: @like_comments, as: :comment, locals: { user: @user } %>
      <%=paginate @like_comments%>
    </div>
  </div>
</div>     
